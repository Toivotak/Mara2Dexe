#pragma once
#include <SDL.h>
#include "../GameInstance.h"

namespace Mara {

	class Event {
	
	protected:
		//SDL_Event event;

	public:
		Event();

		//virtual void GetEvent(SDL_Event event) = 0;

		//virtual ~Event() = 0;
	};
}
